import { SecongPipePipe } from './secong-pipe.pipe';

describe('SecongPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SecongPipePipe();
    expect(pipe).toBeTruthy();
  });
});
